const SingleApp = () => {
    return (
        <Link to={`/appDetails/${id}`}>
            <div>
                <h1>Details</h1>
            </div>
        </Link>
    );
};

export default SingleApp;